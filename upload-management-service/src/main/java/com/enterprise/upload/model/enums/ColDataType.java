package com.enterprise.upload.model.enums;
public enum ColDataType {
    STRING, INTEGER, BIGINT, DECIMAL, BOOLEAN,
    DATE, DATETIME, TIMESTAMP, EMAIL, PHONE,
    JSON, ARRAY, UUID, BASE64, XML_NODE
}
